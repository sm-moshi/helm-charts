Argus has been installed.

To view the running pods:
  kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

To access the service:
  kubectl get svc -n {{ .Release.Namespace }} {{ include "bjw-s.common.lib.chart.names.fullname" . }}

If ingress is enabled, check your Ingress resource:
  kubectl get ingress -n {{ .Release.Namespace }} {{ include "bjw-s.common.lib.chart.names.fullname" . }}
