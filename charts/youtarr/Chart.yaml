apiVersion: v2
name: youtarr
description: Public Helm chart for Youtarr with optional DHI MariaDB
home: https://github.com/DialmasterOrg/Youtarr
type: application
version: 0.1.38
appVersion: "v1.81.1"
kubeVersion: ">=1.28.0-0"
maintainers:
  - name: m0sh1
    email: yael@m0sh1.cc
    url: https://github.com/isityael
sources:
  - https://github.com/DialmasterOrg/Youtarr
  - https://github.com/isityael/helm-charts
keywords:
  - youtarr
  - youtube
  - downloader
  - media
  - mariadb
annotations:
  category: Media
  licenses: MIT
  artifacthub.io/license: MIT
  artifacthub.io/links: |
    - name: Youtarr
      url: https://github.com/DialmasterOrg/Youtarr
    - name: Chart source
      url: https://github.com/isityael/helm-charts/tree/main/charts/youtarr
  artifacthub.io/changes: |
    - kind: changed
      description: Update the advertised Youtarr application version to v1.79.0.
    - kind: changed
      description: Update the advertised Youtarr application version to v1.78.0.
    - kind: fixed
      description: Render secret-backed config override keys in deterministic order.
    - kind: changed
      description: Update the advertised Youtarr application version to v1.77.0.
    - kind: fixed
      description: Keep Renovate-managed MariaDB digests in the dedicated digest value.
    - kind: fixed
      description: Rename the config override key-list environment variable to avoid kube-linter false positives.
    - kind: added
      description: Allow mounting a Secret-backed persistent YouTube cookies file.
    - kind: changed
      description: Parse JSON secret-backed config override values when possible.
    - kind: added
      description: Allow secret-backed config.json overrides through configSecretOverrides.
    - kind: added
      description: Allow config.json overrides through configOverrides.
    - kind: added
      description: Allow configuring the Deployment update strategy.
    - kind: added
      description: Initial Youtarr chart with DHI MariaDB, external database, persistence, and routing support.
