replicaCount: 1
revisionHistoryLimit: 2
strategy: {}

image:
  repository: docker.io/dialmaster/youtarr
  tag: v1.81.1@sha256:3836f1a050e57e8c4d156da8b664f159a837c305b63e299919a29464a852dd1f
  digest: ""
  pullPolicy: IfNotPresent

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

youtarr:
  timezone: UTC
  uid: 1000
  gid: 1000
  logLevel: info
  trustProxy: ""
  dataPath: ""

# Values merged into /app/config/config.json before Youtarr starts.
# Example:
# configOverrides:
#   autoUpdateYtdlp: true
configOverrides: {}

# Secret-backed values merged into /app/config/config.json before Youtarr starts.
# Example:
# configSecretOverrides:
#   jellyfinApiKey:
#     secretName: youtarr-jellyfin
#     secretKey: api-key
configSecretOverrides: {}

cookies:
  existingSecret:
    name: ""
    key: cookies.user.txt
  mountPath: /app/config/cookies.user.txt

auth:
  enabled: true
  presetExistingSecret:
    name: ""
    usernameKey: username
    passwordKey: password

database:
  type: embedded

mariadb:
  enabled: true
  image:
    repository: dhi.io/mariadb
    tag: 12.3.3-debian13
    digest: sha256:4a724bf6c7f1085c7574154498b52718277eda60e7806683044b15ea58cdde1d
    pullPolicy: IfNotPresent
  imagePullSecrets: []
  auth:
    database: youtarr
    username: youtarr
    existingSecret:
      name: ""
      rootPasswordKey: mariadb-root-password
      passwordKey: mariadb-password
  persistence:
    enabled: true
    storageClass: ""
    size: 8Gi
    accessMode: ReadWriteOnce
    existingClaim: ""
  podSecurityContext:
    fsGroup: 65532
    fsGroupChangePolicy: OnRootMismatch
    seccompProfile:
      type: RuntimeDefault
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
    readOnlyRootFilesystem: false
    seccompProfile:
      type: RuntimeDefault
  resources:
    requests:
      cpu: 50m
      memory: 256Mi
    limits:
      memory: 1Gi

externalDatabase:
  host: ""
  port: 3306
  database: youtarr
  username: youtarr
  existingSecret:
    name: ""
    passwordKey: password

serviceAccount:
  create: false
  automount: false
  annotations: {}
  name: ""

podAnnotations: {}
podLabels: {}

podSecurityContext:
  fsGroup: 1000
  fsGroupChangePolicy: OnRootMismatch
  seccompProfile:
    type: RuntimeDefault

securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: false
  seccompProfile:
    type: RuntimeDefault

service:
  type: ClusterIP
  port: 80
  containerPort: 3011

persistence:
  downloads:
    enabled: true
    storageClass: ""
    size: 20Gi
    accessMode: ReadWriteOnce
    existingClaim: ""
    hostPath:
      enabled: false
      path: ""
      type: DirectoryOrCreate
  config:
    enabled: true
    storageClass: ""
    size: 1Gi
    accessMode: ReadWriteOnce
    existingClaim: ""
  jobs:
    enabled: true
    storageClass: ""
    size: 1Gi
    accessMode: ReadWriteOnce
    existingClaim: ""
  images:
    enabled: true
    storageClass: ""
    size: 5Gi
    accessMode: ReadWriteOnce
    existingClaim: ""

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts:
    - host: youtarr.local
      paths:
        - path: /
          pathType: Prefix
  tls: []

httpRoute:
  enabled: false
  annotations: {}
  parentRefs: []
  hostnames: []

probes:
  liveness:
    enabled: true
    httpGet:
      path: /api/health
      port: http
    initialDelaySeconds: 30
    periodSeconds: 20
    timeoutSeconds: 5
    failureThreshold: 6
  readiness:
    enabled: true
    httpGet:
      path: /api/health
      port: http
    initialDelaySeconds: 10
    periodSeconds: 10
    timeoutSeconds: 5
    failureThreshold: 6

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    memory: 1Gi

extraEnv: []

nodeSelector: {}
tolerations: []
affinity: {}
